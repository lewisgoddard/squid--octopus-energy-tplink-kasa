# Use the public npm registry. This also stops Dependabot from aborting with
# `private_registry_config_not_found` for npm.pkg.github.com — its automatic
# GitHub-Packages auth injects that registry, and the npm updater needs a .npmrc
# (or registries config) present. We don't use GitHub Packages.
registry=https://registry.npmjs.org/
